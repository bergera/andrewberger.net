#header
  .container
    .left
      %h2 Andrew Berger
    .right
      %p I'm a #{link_to "martial artist", "http://www.daytonquestcenter.com"}, web developer, and amateur beer and coffee snob. Born and raised in Dayton, OH, I live in Belmont with my cat Pippa.

#info.container
  %dl
    %dt.left Find Me
    - data.index.social_links.each do |link|
      %dd.right.findme{data: {sitename: link.sitename || nil}}<
        %i.icon{class: 'icon-' + link.css_class}
        = link_to link.text, link.url

    - data.index.info_sections.each do |section|
      %dt.left.section-start= section.title
      - section.contents.each.with_index do |text, idx|
        - section_class = idx == 0 ? "section-start" : nil
        %dd.right{class: section_class}= text
